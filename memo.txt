・ActorRuntimeBundle を介した Runtime 抽象への移行は完了。今後は MailboxRuntime → MailboxBuilder/Handle への分割を進めたい。
    ・PriorityScheduler・Guardian・ActorCell など、「R: MailboxRuntime」依存を Runtime 抽象（ActorRuntimeBundle）＋ Builder/
  Handle
    に置き換える。
    ・ReceiveTimeoutFactoryShared のアダプタ導入済み。Timeout 周りの API も Runtime 抽象に合わせて再確認する。
    ・docs/design/2025-10-11-runtime-bundle-plan.md のフェーズ 2 TODO（Mailbox 再分割、設計レビュー）を消化すること。
    ・現時点では `use crate::ReceiveTimeoutScheduler;` が shared.rs に未導入で `cargo test --workspace` が落ちる。忘れず修正す
  る。

  一旦テストが通ってからでよいですがActorRuntimeBundleの中にSchedulerのインスタンスがあるイメージなのですが、実際は逆になってませ
  ん？想定どおりですか？まだコード編集中から途中段階なのかな？
